import React from "react";

const Skills = () => {
  return (
    <div>
      <div>
        React, Javascript, HTML, CSS, Redux, Node, PostgreSQL, MongoDB, Git,
        Heroku
      </div>
    </div>
  );
};

export default Skills;
